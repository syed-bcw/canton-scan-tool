# Copy to .env and edit.

# Scan (defaults already match current repo scripts)
# Typical setup when using kubectl port-forward to a service on port 3128:
# - Keep SCAN_HOST as the TLS hostname
# - Keep SCAN_PORT=3128
# - Keep SCAN_IP=127.0.0.1 so curl uses the local port-forward via --resolve
# SCAN_SCHEME=https
# SCAN_HOST=scan.sv-1.global.canton.network.sync.global
# SCAN_PORT=3128
# SCAN_IP=127.0.0.1
# SCAN_PREFIX=/api/scan

# Validator (placeholder defaults)
# VALIDATOR_SCHEME=https
# VALIDATOR_HOST=localhost
# VALIDATOR_PORT=8080
# VALIDATOR_IP=127.0.0.1
# VALIDATOR_PREFIX=/api/validator

# Common
# API_VERBOSE=1
# API_AUTH_BEARER=YOUR_JWT
# API_CURL_EXTRA="--connect-timeout 2"
