{
  "name": "canton-scan-tools",
  "private": true,
  "version": "0.0.0",
  "description": "Scan-only helper CLI for Canton APIs",
  "license": "UNLICENSED",
  "bin": {
    "canton-scan": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "npm run build",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
